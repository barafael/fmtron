(
    tasks: {
        "debug message": Dbg(
            msg: "test message. some text after it.",
        ),
        "shell command": Shell(
            command: "ls",
            args: Some(["-l", "-h"]),
            ch_dir: Some("/"),
        ),
    },
)
